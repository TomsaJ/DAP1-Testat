public class Testat
{

    public static Fraction apply( Fraction[] arr, Fraction x )
    {
        int f;
        int numerator;   
        int denominator;
        if (arr.length >= 2)
        {
            
            for ( int i = 0 ; i < arr.length; i++)
            {arr[i].add(arr[i].add(arr[i].add(arr[i]))); 

            } 
        }
        else 
        {

        }
        return new Fraction ();

    }
}